// Section entrance animations and pill stagger are handled entirely by CSS.
// No JS animation-delay overrides — setting animationDelay on a live animation
// forces a restart, causing a flash of opacity:0 before the re-run.
//
// theme.js intentionally left minimal so it doesn't interfere with CSS timing.
